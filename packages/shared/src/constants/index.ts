export enum Role {
  ADMIN = 'admin',
  EDITOR = 'editor',
  VIEWER = 'viewer',
}

export enum Permission {
  VIEW = 'view',
  EDIT = 'edit',
  DELETE = 'delete',
  MANAGE = 'manage',
}

export enum EntityType {
  BOOK = 'book',
  CHAPTER = 'chapter',
  PAGE = 'page',
}
